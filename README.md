# PayHug 투자자 어드민 — 용어 해설

투자자 어드민 화면에 나오는 용어 50건을 기호·산식·등장 화면·연관 용어 다섯 갈래로 풀어 둔 단독 배포본. `index.html` 한 파일과 문서가 부르는 자산만 있고 바깥으로 나가는 링크는 없다. 본문의 `invest-assets.html` 같은 코드 표기는 링크가 아니라 화면 이름이다. 표기 금액·요율·상호는 전부 예시이고 요율·산식은 미확정 사안이다. 원본은 `Joo2n/payhug-investor-admin`의 `glossary.html`이며, 여기 있는 `index.html`은 그것을 변환해 찍어 낸 결과다. 고칠 내용이 있으면 원본 쪽을 고친 뒤 `payhug-spec/_pipeline/investor_admin/sync_glossary.sh`를 돌린다. 이 저장소에서 직접 고친 것은 다음 동기화에서 사라진다.
